<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags always come first -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.2/css/bootstrap.min.css">
  	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
  </head>
  <body>
    <div style="padding-bottom:70px;">
    <nav class="navbar navbar-dark navbar-fixed-top bg-primary">
      <a class="navbar-brand" href="/"> My Website</a>
      <ul class="nav navbar-nav">
        <li class="nav-item active">

        </li>
        {{#if user}}
        <li class="nav-item pull-xs-right">
          <div class="dropdown">
            <button class="btn btn-primary dropdown-toggle" style="margin-left:75px;" type="button" data-toggle="dropdown">{{user.username}}
            <span class="caret"></span></button>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="/account/profile"><span class="fa fa-user"></span> My Account</a></li>
              <hr>
              <li><a class="dropdown-item" href="/logout">Logout</a></li>
            </ul>
          </div>
        </li>
        {{else}}
        <li class="nav-item pull-xs-right">
          <a type="button" class="btn btn-success-outline" href="/signup">Sign Up</a>
        </li>
        <li class="nav-item pull-xs-right">
          <a class="nav-link" href="/login">Login</a>
        </li>
        {{/if}}
      </ul>
    </nav>
    </div>
    {{{body}}}
    <!-- jQuery first, then Bootstrap JS. -->
    <script src="http://code.jquery.com/jquery-latest.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.2/js/bootstrap.min.js"></script>
  </body>
</html>
